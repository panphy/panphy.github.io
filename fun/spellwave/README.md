# Spellwave

Three.js typing combat game for low-pressure exposure to GCSE physics keywords, units, definitions, and later formula prompts.

Run it from the repository root with a simple local server:

```bash
python3 -m http.server 8000
```

Then open `/fun/spellwave.html`.

This page is published under `/fun` and participates in the production service-worker offline cache.
